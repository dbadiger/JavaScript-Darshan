const Id = 123
let email ="Darshan@google.com"
var name = "Darshan"
city = "Hubli"
let state;
console.table([Id, email, name, city, state]);

/*
const - it is used to assign constant values. like unique IDs,these cannot be changed.

var - prefer not to use var. because of issues in block scope and functional scope.

undefined - variable is declared but value is not assigned, then that variabel is undefined.


*/